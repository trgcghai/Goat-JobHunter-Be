<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-5.0.xsd"
        objectQuotingStrategy="QUOTE_ONLY_RESERVED_WORDS">

    <!-- ================================================= -->
    <!-- Create sequence for chat_member.member_id if missing -->
    <!-- ================================================= -->
    <changeSet id="chat-member-create-seq-1" author="HP">
        <preConditions onFail="MARK_RAN">
            <not>
                <sequenceExists sequenceName="chat_members_seq"/>
            </not>
        </preConditions>

        <createSequence
                sequenceName="chat_members_seq"
                startValue="1"
                incrementBy="1"/>
    </changeSet>

    <!-- ================================================= -->
    <!-- Set default value for member_id column -->
    <!-- ================================================= -->
    <changeSet id="chat-member-set-default-2" author="HP">
        <preConditions onFail="MARK_RAN">
            <and>
                <tableExists tableName="chat_members"/>
                <columnExists tableName="chat_members" columnName="member_id"/>
            </and>
        </preConditions>

        <addDefaultValue
                tableName="chat_members"
                columnName="member_id"
                defaultValueSequenceNext="chat_members_seq"/>
    </changeSet>

</databaseChangeLog>
