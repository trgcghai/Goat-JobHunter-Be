<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.23.xsd">

    <changeSet id="enable-citext-extension" author="dat">
        <preConditions onFail="MARK_RAN">
            <not>
                <sqlCheck expectedResult="1">
                    SELECT COUNT(*) FROM pg_extension WHERE extname = 'citext'
                </sqlCheck>
            </not>
        </preConditions>

        <sql>
            CREATE EXTENSION IF NOT EXISTS citext;
        </sql>
    </changeSet>

    <!-- 2. Change column type VARCHAR -> CITEXT -->
    <changeSet id="company-name-varchar-to-citext" author="dat">
        <preConditions onFail="MARK_RAN">
            <columnExists tableName="companies" columnName="name"/>
        </preConditions>

        <modifyDataType
                tableName="companies"
                columnName="name"
                newDataType="CITEXT"
        />
    </changeSet>

</databaseChangeLog>
